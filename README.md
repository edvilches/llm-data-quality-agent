# llm-data-quality-agent

An open-source LLM-powered data quality assistant that runs locally using Ollama.

## Configuration

1. Copy the example config:

cp config/config.example.json config/config.json

2. Edit config.json with your local settings
